# API Service

FastAPI service that routes questions to governed metrics and executes them against Databricks/DBT artifacts. See `pyproject.toml` for dependencies and configure environment via `.env`.
